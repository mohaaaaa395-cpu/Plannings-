{
  "name": "cedif-planning",
  "version": "1.0.0",
  "description": "Application de génération automatique de plannings — CEDIF Saint-Antoine",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && node src/index.js",
    "migrate": "cd backend && npm run migrate",
    "seed": "cd backend && npm run seed",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "test": "cd backend && node test/engine.test.mjs && node test/coverage.test.mjs && node test/rest.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
